services:
  springboot-connect:
    build: .
    image: springboot-connect:latest
    container_name: springboot-connect
    ports:
      - "8080:8080"
      - "9090:9090"
      - "9091:9091"
    restart: unless-stopped
